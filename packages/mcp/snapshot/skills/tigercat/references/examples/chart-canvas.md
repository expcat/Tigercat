---
name: tigercat-examples-charts
description: Compact Tigercat Charts Vue and React usage routes
---

<!-- generated by pnpm docs:api -->

# Charts Examples

图表组件共享数据、series、legend、tooltip 和 axes 模式，细节看 chart 类型源。

每个组件一节，供 MCP `tigercat_component` 按 `## {Component}` 抽取。绑定差异见 `shared/patterns/common.md`。

## ChartCanvas

Runnable modules: `examples/example/vue3/src/examples/chart-canvas/` and `examples/example/react/src/examples/chart-canvas/`.

Note: `responsive` 观察画布自己的宿主，不是 legend 壳。默认 padding 盖住 ChartAxis 标签。有 `title` / `aria-label` 时 svg 是 `role="img"`。render props / 作用域槽给出 `innerRect`。

Vue: `<ChartCanvas title="Sales" :width="320" :height="200"><slot /></ChartCanvas>`

React: `<ChartCanvas title="Sales" width={320} height={200}>{plot}</ChartCanvas>`

Imports: use PascalCase subpaths such as `@expcat/tigercat-vue/Button` and `@expcat/tigercat-react/Button`. Hooks and `notification` use the same subpath rule. Shared types and helpers come from `@expcat/tigercat-core`.
