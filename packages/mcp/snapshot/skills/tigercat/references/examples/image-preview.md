---
name: tigercat-examples-basic
description: Compact Tigercat Basic Vue and React usage routes
---

<!-- generated by pnpm docs:api -->

# Basic Examples

Vue/React API 基本同名；React 使用 `className`，Vue 使用 `class` 或透传 attrs。

每个组件一节，供 MCP `tigercat_component` 按 `## {Component}` 抽取。绑定差异见 `shared/patterns/common.md`。

## ImagePreview

Runnable modules: `examples/example/vue3/src/examples/image-preview/` and `examples/example/react/src/examples/image-preview/`.

Note: `images` 必填（`string | { src, alt? }`）。未传 `open` 视为关（Alert 相反：省略 `open` 是展示）。缩放用 `minScale`/`maxScale`。到头 disable；空列表关闭。

Vue: `<ImagePreview :images="images" />`

React: `<ImagePreview images={images} />`

Imports: use PascalCase subpaths such as `@expcat/tigercat-vue/Button` and `@expcat/tigercat-react/Button`. Hooks and `notification` use the same subpath rule. Shared types and helpers come from `@expcat/tigercat-core`.
