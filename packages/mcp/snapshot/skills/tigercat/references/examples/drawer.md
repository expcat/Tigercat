---
name: tigercat-examples-feedback
description: Compact Tigercat Feedback Vue and React usage routes
---

<!-- generated by pnpm docs:api -->

# Feedback Examples

弹层类组件通常使用 `open`/`update:open` 或 `open`/`onOpenChange`。

每个组件一节，供 MCP `tigercat_component` 按 `## {Component}` 抽取。绑定差异见 `shared/patterns/common.md`。

## Drawer

Runnable modules: `examples/example/vue3/src/examples/drawer/` and `examples/example/react/src/examples/drawer/`.

Note: 与 Modal 同一套在场、焦点栈和关场。打开时焦点进对话框或 `initialFocus`。`placement` 含 `start`/`end`，先换成物理边再决定滑动方向；`fullscreenOnMobile` 铺满后，关闭滑动跟铺满后的边。swipe 只在标题栏或对应轴滚到头时成立。面板类名只有 `className`。`bodyPadding={false}` 去掉默认内边距，自定义间距用 `bodyClassName`。离开回调等这一层过渡结束。关闭名走 `locale.drawer`。嵌套 Drawer 进外层 overlay-host，Esc 先关里层。

Vue: `<Drawer :open="open" title="Title">Body</Drawer>`

React: `<Drawer open={open} title="Title">Body</Drawer>`

Imports: use PascalCase subpaths such as `@expcat/tigercat-vue/Button` and `@expcat/tigercat-react/Button`. Hooks and `notification` use the same subpath rule. Shared types and helpers come from `@expcat/tigercat-core`.
