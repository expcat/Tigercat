/**
 * Kanban types live on the TaskBoard model in `task-board.ts`.
 * This module re-exports the public names so existing source-path imports keep working.
 */

export type { KanbanProps, KanbanSwimlane } from './task-board'
