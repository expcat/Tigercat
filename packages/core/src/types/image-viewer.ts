/**
 * ImageViewer types live next to ImagePreview in `image.ts`.
 * This module re-exports the public name so existing source-path imports keep working.
 */

export type { ImageViewerProps } from './image'
