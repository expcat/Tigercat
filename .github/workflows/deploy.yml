name: Deploy GitHub Pages

on:
  push:
    tags:
      - 'v*'
  workflow_run:
    workflows: ['Create Release Tags']
    types:
      - completed
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Vue example
        run: pnpm --filter @expcat/tigercat-example-vue3 build

      - name: Build React example
        run: pnpm --filter @expcat/tigercat-example-react build

      - name: Prepare public_html
        run: |
          mkdir -p public_html
          cp examples/index.html public_html/index.html
          cp examples/example/vue3/dist/index.html examples/example/vue3/dist/404.html
          cp examples/example/react/dist/index.html examples/example/react/dist/404.html
          mv examples/example/vue3/dist public_html/vue
          mv examples/example/react/dist public_html/react

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: public_html

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
