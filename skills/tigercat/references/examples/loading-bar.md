---
name: tigercat-examples-feedback
description: Compact Tigercat Feedback Vue and React usage routes
---

<!-- generated by pnpm docs:api -->

# Feedback Examples

弹层类组件通常使用 `open`/`update:open` 或 `open`/`onOpenChange`。

每个组件一节，供 MCP `tigercat_component` 按 `## {Component}` 抽取。绑定差异见 `shared/patterns/common.md`。

## LoadingBar

Runnable modules: `examples/example/vue3/src/examples/loading-bar/` and `examples/example/react/src/examples/loading-bar/`.

Note: `error` 和 `finish` 共用 `start` 计数。还有未结束的 `start` 时保持加载，全部结束再进入成功或失败然后隐藏。开始、失败、结束各说一次，百分比留在 `progressbar` 上。

Vue: `LoadingBar.start()`

React: `LoadingBar.start()`

Imports: use PascalCase subpaths such as `@expcat/tigercat-vue/Button` and `@expcat/tigercat-react/Button`. Hooks and `notification` use the same subpath rule. Shared types and helpers come from `@expcat/tigercat-core`.
