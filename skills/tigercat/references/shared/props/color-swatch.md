---
name: tigercat-props-form
description: Compact generated Tigercat Form props reference
---

<!-- generated by pnpm docs:api -->

# Form Props

表单输入、选择、校验和上传相关组件。 共 1 个组件。字段细节以 `packages/core/src/types/*.ts` 为准；跨包组件以本段列出的源码为准。

## ColorSwatch

`packages/core/src/types/color-swatch.ts` · `ColorSwatchProps` · 4/19 props

Note: 选中比较走和取色器同一套解析。`#fff` 与 `#ffffff`、hex 与 `rgb()` 可以是同一支。解析不了的字符串不涂成颜色。只读用 `readOnly`。

| Prop            | Type                       | Default | Notes |
| --------------- | -------------------------- | ------- | ----- |
| `value?`        | `string`                   | `-`     | -     |
| `defaultValue?` | `string`                   | `-`     | -     |
| `colors?`       | `ColorSwatchOptionInput[]` | `-`     | -     |
| `groups?`       | `ColorSwatchGroup[]`       | `-`     | -     |

Events/callback props: `onChange?`.
