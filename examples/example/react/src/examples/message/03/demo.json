{
  "id": "message-03",
  "title": "异步流程",
  "description": "同一个 key 会替换那一条。本例关掉 loading 再开一条 success。不传 duration 的 loading 不自动关；传入的有限时长仍会到时关闭。本例在完成后手动 close。",
  "entry": "App.tsx",
  "order": 3,
  "viewport": {
    "mode": "auto",
    "minHeight": 120,
    "maxHeight": 720
  }
}
