// NOTE: This file was used for one-off debugging of demo runtime behavior.
// Keeping it as a skipped test suite avoids coupling the test suite to demo pages,
// while also preventing Vitest from failing with "No test suite found".

import { describe, it } from 'vitest'

describe('Modal demo runtime (debug)', () => {
  it.skip('debug-only placeholder', () => {})
})
