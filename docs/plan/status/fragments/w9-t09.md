# W9 T09 E1–E13

Legend click toggles a hidden set in `useChartInteraction` and does not change the selection index. `buildChartLegendItems` accepts `isHidden`. Bar legends are one series item. Waterfall and Sankey are new components: `layoutWaterfall` / `layoutSankey` in core, both frameworks bind marks, and export calls `downloadChartSvg`. Funnel labels include layer ratios from `funnelLayerRatios`.

Partial: grouped/stacked bandwidth, shared axis, time axis, brush, structured tooltip, pie label collision, rose radius, radar per-indicator max, gauge pointer versus arc, heatmap band, sunburst/treemap drill breadcrumb, gantt milestone drag, and org-chart find/zoom are core functions without a full view binding.
