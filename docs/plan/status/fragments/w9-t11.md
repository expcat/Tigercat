# W9 T11 E3 E4 E5 E6 E7 E9

E5 `NotificationBell` is a new component: unread count, `NotificationCenter` in `Popover`, focus return, one live announcement, `item-read-change`. E6 `FormWizard` awaits `autoSave` before committing a step and asks `confirmModal` when `model` differs from the step snapshot. E7 schema fields with `readOnly` render text and skip rules; `widgetParams` is narrowed by widget. E9 `AssigneePicker` searches caller options and emits selected ids with a local listbox.

Partial: E3 activity date groups and E4 comment edit/delete emit paths are core helpers (`groupActivityByDate`, `commentEditRequest`) without a full view binding.
