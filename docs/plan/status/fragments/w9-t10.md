# W9 T10 E1–E8

`VirtualList` `columns` and `orientation` call `virtualGridWindow` (`calculateVirtualRange`). Sticky indexes stay mounted. Find/replace, brackets, markdown TOC, rich-text slash nodes, file permissions, annotation undo, drag announcements, and print page sizes are functions in `w9-data.ts`.

Partial: code editor, markdown editor, rich text, file manager, image annotation, drag preview, and print preview UI are not fully rebound onto those functions.
