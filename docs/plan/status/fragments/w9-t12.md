# W9 T12 E2

`packages/cli` `add` writes the example `App.vue` / `App.tsx` for the named component when that file exists, otherwise a public-prop usage snippet. Unknown names exit 1 and print `Available` from `ALL_COMPONENTS`. Each resolved component still gets one sample file.
