status: done
wave: W7
stage: feedback-overlay
started: 2026-09-23T21:24:49+08:00
finished: 2026-09-23T23:37:00+08:00
pid: 2565397
grok_task: W7-feedback-overlay
items:
  - T05 O1: done — omitting open dismisses on close and duration and moves focus forward; controlled open only emits; the countdown bar uses the shared remaining ratio and reduced motion pauses both
  - T05 O2: done — spinning re-arms delay; fullscreen loading is z-1150 above modal and joins the one focus stack; region focus is captured before inert and restored after; aria-busy is on the region
  - T05 O3: done — hover click/Enter opens and does not toggle closed; pointer leave keeps the popup open while the trigger is focused; disabled closes immediately; tooltip warns on focusable content
  - T05 O4: done — OK waits on a Promise, preventDefault, or false and stays open on rejection; Vue reads the listener return; only the dialog body scrolls
  - T05 O5: done — Popconfirm uses the shared click controller, Tab leaves closes it, and a pending promise blocks Escape, outside click, and another trigger click
  - T05 O6: done — Message and notification only mutate the core queue rendered by the current ConfigProvider; the deepest provider wins; the same key replaces; a throwing onClose still removes the toast
  - T05 O7: done — the target scrolls once per open and step; the default hole is visual and the target stays inert; interact is the only click-through; loadSteps runs once per open
  - T05 O8: done — new non-error alerts use their own status; popover title is the name; modal, drawer, and tour focus the dialog or initialFocus; loading bar announces start, finish, and error
  - T05 O9: done — error and finish share the start count; a nested error stays loading until every start has finished
  - T05 O10: done — Message is closable by default; pointer and focus each hold the timer; the primary notification action is one button, not a card click
  - T05 O11: done — a fullscreen mobile drawer swipes along the writing direction; start and end become a physical edge before the swipe axis
  - T05 O12: done — dragging sets inline transitionDuration 0ms so the dialog follows the pointer without the transition shorthand
  - T05 O13: done — close returns the uncontrolled index to the first non-skipped step, including loaded steps, and emits that index when current is controlled
  - T05 O14: done — open Modal, Drawer, Tour, and declarative toast containers render into the provider outlet that exists in server HTML; imperative calls do nothing on the server
  - T05 O15: done — open state, hover timing, outside click, and Escape live in createOverlayPopupController; Vue and React use-popup only bind refs and events
  - T05 O16: done — progress stripes and reduced motion live in the Tailwind plugin and merge with the shared reduced-motion block; components do not inject a style tag
  - T05 O17: done — popover width is one positive pixel number or one CSS length; a bad value keeps the default max-width
  - T05 O18: done — the panel has only className; bodyPadding is boolean; custom spacing is bodyClassName; panelClassName is gone
  - T05 O19: done — after-close waits for this layer's transition end; reduced motion fires immediately; scheduleOverlayLeave is not used
  - T05 O20: done — createNotificationStackUpdateScheduler is deleted; appear and leave use stylesheet transitions
  - T05 O21: done — message and notification placement is one set of logical classes, with no translateX companion
tests: node 22 vitest, 49 files, 723 passed (core alert/loading/overlay/floating/focus/imperative/drawer/tour/progress/i18n plus Vue and React Alert, Loading, Tooltip, Popover, Popconfirm, Modal, Drawer, Tour, Message, Notification, LoadingBar, Progress, overlay positioning). tsc -p packages/core --noEmit passed. pnpm docs:api and api baseline regenerated.
outputs_summary: W7 feedback overlay matches T05 O1–O21 on Vue and React; skills, examples, and MIGRATION-3.0 notes updated.
blocked_reason:
notes: W7 only. Do not start W8. No bot-to-bot messages. Resumed after max-turns (prior pid 2530416).
