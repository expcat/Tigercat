status: done
wave: W6
stage: virtual-read-correct
started: 2026-09-23T18:20:09+08:00
finished: 2026-09-23T20:55:57+08:00
pid:
grok_task: W6-virtual-read-correct
items:
  - T10 O1: done — `calculateVirtualRange` uses ceil((scrollTop + viewport) / itemHeight) so the bottom-edge row is included; column scroll goes through `getLogicalInlineScroll`.
  - T10 O2: done — `dynamicSizeStrategy` stores measured heights by item id and VirtualList calls `setItemKeys`.
  - T10 O3: done — `createInfiniteScrollFlight` is the only in-flight gate; Vue InfiniteScroll now uses it the same way as React.
  - T10 O9: done — VirtualList stays out of tab order unless it has an accessible name; `scrollToIndex` accepts `auto`.
  - T10 O10: done — preload `rootMargin` and inverse anchoring follow writing direction on both InfiniteScroll ends.
  - T08 O1: done — CSV/Markdown/xlsx text trims leading whitespace before formula-prefix neutralization in `data-export-value`.
  - T08 O2: done — Calendar and DatePicker today/month use the caller `now`; `selectToday` does not call `new Date()`.
  - T08 O3: done — VirtualTable arrow keys update the active row and focus that row on both ends.
  - T08 O4: done — Table virtual scrollTop and viewport come from the inner scroller; declared row height; a page that fits is not windowed.
  - T08 O5: done — fixed header cells stack above fixed body cells via the shared z-index helpers.
  - T08 O16: done — column windows use measured widths; row boxes do not clip the focus ring.
  - T08 O22: done — `VirtualTableColumn` omits `sortable` and `filter`.
  - T09 O1: done — pie sweep defaults to start angle plus 2π.
  - T09 O2: done — Gantt `showToday` uses caller `now` and draws no today line without it.
  - T09 O11: done — point index and bar category keep the pre-filter data key in the shared layout.
  - T09 O12: done — finite min/max is one `scanFiniteExtent` pass shared by the chart layouts.
  - T09 O18: done — `serializeChartSvg` drops URLs rejected by `link-utils`.
  - T11 O1: done — ChatWindow `role="log"` is off the virtual scroller; the live region is a stable node.
  - T11 O2: done — stick-to-bottom anchors the last message id; long threads virtualize by default on both ends.
  - T11 O19: done — chat, activity, comments, and notifications format clocks with one caller timezone and omit host-varying times on the first paint.
  - T11 O20: done — activity, comments, and notifications window lists at 40 rows by id; inactive notification groups do not mount a list; page loads use the single in-flight request outside the feed.
  - T04 O23: done — Cascader and TreeSelect `virtual` follow the active item on both ends; AutoComplete, Mentions, and Transfer use the same window once the list exceeds the panel.
tests: vitest run of InfiniteScroll, ChatWindow, ActivityFeed, CommentThread, NotificationCenter, Table, VirtualList, VirtualTable, Cascader, TreeSelect, AutoComplete, Mentions, Transfer, Calendar, Gantt, PieChart, DatePicker (vue+react as present) plus core infinite-scroll, chat-window, cascader, virtual-list, virtual-table, chart-export, data-export, composite-time. 437+444+173+120 passed across those batches after fixes; the last mentions/transfer/autocomplete batch was 120 passed.
outputs_summary: W6 virtual window, caller clock, export sanitize, and both-end wiring are in the tree; status is done.
blocked_reason:
notes: W6 only. Do not start W7. No bot-to-bot messages. Resumed after max-turns.
