status: done
wave: W4
stage: form-complex-tree
started: 2026-09-23T15:26:00+08:00
finished: 2026-09-23T16:22:00+08:00
pid:
grok_task: W4-form-complex-tree
items:
  - T04 O1: done — 展示可本地化；解析用同一套历法和数字回到公历；`name` 提交 `YYYY-MM-DD`（范围 `start|end`）。
  - T04 O2: done — 点选、键入、快捷方式和确定共用禁用边界；结束早于开始会对调；未完成的区间留在面板里。
  - T04 O3: done — 此刻、键入和确定共用 min/max/disabledTime；结束早于开始会对调；`showSeconds` 为假时模型不留秒。
  - T04 O4: done — 颜色文本在失焦或 Enter 时提交；解析失败不上屏，隐藏域提交空。
  - T04 O5: done — Cron 的表单值是空或一份合法 5 段表达式；空字段不画成五个 `*`。
  - T04 O6: done — 数字键盘打开后焦点在对话框里，当前键有 roving tabindex 和可见样式。
  - T04 O7: done — 级联、树选、自动完成、穿梭和上传里，缺键、`null` 和 `''` 是同一种空。
  - T04 O8: done — 级联、树和穿梭只按字符串键身份比较一次；对不上的级联段保留原始键。
  - T04 O9: done — 勾选框表示级联；`checkStrategy` 只决定提交哪些键；半选下一次点击补全这一支。
  - T04 O10: done — 自动完成关层时提交或还原；原生字段提交已提交的值。
  - T04 O11: done — 提及按当前选区插入；解析和插入共用同一条分隔规则。
  - T04 O12: done — 上传的界面、进度、提交和表单写入用同一份列表。
  - T04 O13: done — 签名的值是空或当前笔画的 SVG；Escape 和 pointercancel 丢掉当前笔。
  - T04 O14: done — 级联只在打开时初始化浏览路径，打开期间不因 options 换引用而打回。
  - T04 O15: done — 树选搜索零命中时列表长度为 0，并显示 emptyText。
  - T04 O16: done — 时间面板的桌面列和手机 select 同时在文档里，用 CSS 切换显示。
  - T04 O17: done — 取色拖动只更新预览，松手再写入表单。
  - T04 O18: done — Vue 颜色面板、色板和 Cron 的可访问 id 只走 `useId`。
  - T04 O19: done — 色板按和取色器同一套解析判断选中。
  - T04 O20: done — 日期、时间和取色的打开按钮带 `aria-expanded`、`aria-haspopup="dialog"` 和 `aria-controls`。
  - T04 O21: done — 这些复杂控件只留 `readOnly`；禁用不提交，只读提交，空值仍有一个明确字段。
  - T04 O22: done — 数字键盘进入的值先按模式收成合法串，收不干净当空，不再在非法前缀后拼接。
  - T04 O24: done — 懒加载有名字、失败可重试，鼠标、Enter 和方向键走同一道。
  - T04 O25: done — 上传的选择、类型、预览和重试接在同一份列表上；`name` 与 `fileFieldName` 分开。
  - T04 O26: done — 穿梭的搜索、全选和移动只处理当前可见项；滤掉的已选单独成条。
  - T04 O27: done — 自动完成和提及打开着的空列表仍报告 `aria-expanded`。
  - T04 O28: done — `defaultExpandAll` 只在树从空变为有数据时生效。
  - T04 O29: done — Vue 值事件只留 `update:modelValue`（上传是 `update:fileList`）；React 只留 `onChange`。
  - T04 O30: done — `levelLabel`、`backText`、展开和收起文案在 `locale.cascader` 与 `locale.treeSelect`，离开 Select。
  - T04 O31: done — 数字键盘、签名和自动完成的外部值先收成自己的合法形状，收不干净当空。
  - T07 O8: done — 树搜索零命中是空列表，与 T04 O15 同一份 `getVisibleTreeItems`。
  - T07 O18: done — 树勾选只在 treeitem 上说一次；`posinset` / `setsize` 按兄弟计算。
  - T07 O23: done — 每棵树的拖拽 `containerId` 按实例生成，不再写死 `tree`。
tests: `./node_modules/.bin/vitest run` 52 files, 1054 passed (date/time, color/cron/keyboard, tree/cascader/treeselect, autocomplete/mentions/upload/signature/transfer, i18n, select, docs-api). After two type fixes, NumberKeyboard and TreeSelect specs re-ran: 4 files, 60 passed. `tsc -p packages/core`, `packages/vue`, and `packages/react` `--noEmit` passed after rebuilding `@expcat/tigercat-core`. `node ./scripts/check-public-types.mjs` passed.
outputs_summary: W4 的 33 条复杂表单与树优化已落到 core 与 Vue/React，skill、examples 和 MIGRATION-3.0 已同步；T04 O23 未做。
blocked_reason:
notes: W4 only. Do not start W5. No bot-to-bot messages. T04 O23 virtual window was not changed.
