status: done
wave: W2
stage: protocol-registry
started: 2026-09-23T11:58:00+08:00
finished: 2026-09-23T13:47:15+08:00
pid:
grok_task: W2-protocol-registry
items:
  - T02 O1: done — `link-utils` is the only protocol list (`http`/`https`/`mailto`/`tel`); disabled and `javascript:`/`data:`/`vbscript:` omit `href`; Highlight’s public API is strings only.
  - T07 O1: done — Anchor, Breadcrumb, ScrollSpy, PageHeader (through Link), Dropdown, ContextMenu, Menu, NavigationMenu, and schema `path`/`href`/`iframeSrc` call that gate; `_blank` gets `noopener` and `noreferrer`.
  - T09 O3: done — OrgChart resolves `avatar` with `resolveLinkHref` before writing an SVG `image`; a rejected address renders no image.
  - T12 O2: done — MCP defaults to the packaged skill snapshot; an https mirror must match version and digest; decoded `..` is rejected and redirects are not followed.
  - T12 O3: done — tag publish takes `TAG` from the environment with a full-match regex; branch publish and commit-count tagging exit 1; no `--no-git-checks`; Pages `/mcp/` is checked out from that tag.
  - T12 O4: done — `scripts/rimraf.mjs` deletes only the preset dist, `node_modules`, coverage, and example build directories, and refuses absolute paths and `..`.
  - T12 O5: done — CLI add/generate read `public-components.mjs`; `generate docs` does not scan types; docs are written only by `pnpm docs:api`.
  - T12 O6: done — CLI and MCP versions are their package versions; doctor requires that major, including unwrapped `catalog:` ranges.
  - T12 O7: done — one record supplies the component name, category, type module, package target, doc route, and test group; `types/composite.ts` is gone.
  - T12 O8: done — `exports:check` reports core and framework `exports` match the generated entry table.
  - T12 O9: done — the baseline is the export graph (root and subpaths) and is stable; over-budget form/navigation props are split by component; `validate-api --json` writes under the system temp directory.
  - T12 O10: done — CLI writes stay inside the target project; templates include `.gitignore` and `packageManager`; install uses the locked version; playground starts `pnpm exec vite` from an argument array.
  - T12 O11: done — route hits are capped; `maxBytes` is clamped to 200–50000 and `limit` to 1–30; usage `framework` is only `react` or `vue`.
  - T12 O12: done — `dev:check` requires the files named by package `exports` (passed on Node 22.13 / pnpm 11.9); `validate-tests` fails on skip, missing a11y, and warnings (522 files, 0 warnings).
  - T12 O13: done — reference-style doc links are checked and must stay in the repo; example index and per-component DemoPage coverage passed; SSR markers for Button, DatePicker, and BarChart passed and `.next`/`.output` were removed.
  - T01 O12: done — compat re-exports, `version` constants, and `internal/*-styles` are off the public type surface; framework components import those style modules directly. `tsc --noEmit` passed for core, vue, and react.
tests: tsc --noEmit core/vue/react passed. vitest 19 files / 314 tests (link, highlight, cli, mcp, org chart, breadcrumb, menu schema, routing) passed. release:check, exports:check, api:validate, validate-tests (522), example index, example sources, doc links, dev:check (Node 22), and SSR smoke passed. Baseline and docs generators are idempotent.
outputs_summary: W2 protocol gate, component record, and publish-safety invariants are in place; skills, examples, and migration notes match them.
blocked_reason:
notes: W2 only. Do not start W3. No bot-to-bot messages. Resumed after the prior session hit max turns; closed the style-boundary typecheck, form props index collision, skill notes, and test gate.
