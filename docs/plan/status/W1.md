status: done
wave: W1
stage: primitives
started: 2026-09-23T10:40:00+08:00
finished: 2026-09-23T11:48:00+08:00
pid:
grok_task: W1-primitives
items:
  - T01 O5: done — CSV、Markdown、xlsx 共用一条单元格规则；超限抛出 DataExportLimitError；下载在下一轮再 revoke。
  - T01 O6: done — createFocusScope 是唯一焦点范围：顺序 Tab、离开拉回、最上层 Escape、下层 inert、关闭恢复焦点和滚动。
  - T01 O8: done — 复制只走异步 Clipboard API；失败返回 false，不搬焦点。
  - T01 O9: done — live region 由调用方持有；assertive 只有 aria-live；devWarn 在明确非生产环境输出并限制 200 条。
  - T01 O10: done — 表头高度来自 DOM；空窗口每次是新对象；行高只接受有限正数；尺寸观察走 observeSize。
  - T01 O11: done — 富文本一条消毒写入；target=_blank 始终带 noreferrer noopener；工具栏图标是 path。
  - T01 O16: done — 二维码格式位落在第 8 列和第 8 行右侧；独立解码测试不复用编码器坐标。
  - T01 O17: done — 数字规则比数值；纯空白算空；身份证校验生日和校验位；日期只收 YYYY-MM-DD；url 接受 localhost；路径拒绝 __proto__、constructor、prototype。
  - T01 O18: done — 上传队列可取消；同一 uid 只有一个在途请求；本会话记住已完成分片。
  - T01 O19: done — 每个 createListReorderController 持有自己的拖拽会话。
  - T01 O20: done — 水印覆盖用计算样式看透明度、可见性、尺寸和 background-image；观察器不是删不掉的保证。
  - T01 O21: done — 高亮器返回 token；Code 与 CodeEditor 画成文本节点。
tests: ./node_modules/.bin/tsc -p packages/core/tsconfig.json --noEmit passed. vitest 64 files, 1257 passed (core primitives plus Vue/React modal, drawer, code, editor, QR, watermark, upload, rich text, markdown, virtual table, drag, message, notification, config provider).
outputs_summary: W1 的 12 条原语按新结构落地，skill、examples 与 docs/MIGRATION-3.0.md 已同步。
blocked_reason:
notes: W1 only. Do not start W2. Imperative Message, Notification, and LoadingBar read the document-owner locale so toast layers keep ConfigProvider copy without a process-wide stack.
