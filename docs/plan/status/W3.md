status: done
wave: W3
stage: form-basics
started: 2026-09-23T13:52:30+08:00
finished: 2026-09-23T14:55:00+08:00
pid:
grok_task: W3-form-basics
items:
  - T03 O1: done — `required` and `requiredWhen` are rules; the asterisk is `aria-hidden`.
  - T03 O2: done — a trigger pass keeps errors from rules it did not run.
  - T03 O3: done — one keystroke writes the form once; history keeps the previous present by reference.
  - T03 O4: done — a named FormItem Radio writes its option value, the same value a RadioGroup uses.
  - T03 O5: done — item rules and form rules are one list; renaming a field drops the previous name.
  - T03 O6: done — the first OTP cell accepts the whole code; the other cells only display it.
  - T03 O7: done — a missing key, `''`, and `null` are empty; widgets read that value and do not keep a private one.
  - T03 O8: done — Select highlight and focus settle after paint; Enter creates only an exact unmatched query.
  - T03 O9: done — multi-select values are tags; Backspace removes the last tag; clear emits `null` and stays out of tab order.
  - T03 O10: done — a failed submit focuses the first focusable `[aria-invalid="true"]` control, not the group root.
  - T03 O11: done — `setFieldError` writes a field error; CropUpload type, size, and confirm failures use it and still emit `error`.
  - T03 O12: done — error text stays in the document; field checks are `role="status"`, a failed submit is `role="alert"`.
  - T03 O13: done — disabled is applied on the control; a disabled or loading form does not submit.
  - T03 O14: done — controlled values are written after render; Vue emits `update:modelValue` and does not mutate the prop.
  - T03 O15: done — a new `validate()` supersedes the in-flight one with `FormValidationSupersededError`.
  - T03 O16: done — `gt` / `gte` / `lt` / `lte` accept only finite numbers; `''` is not 0.
  - T03 O17: done — the spinbutton name comes from the visible label or the caller `aria-label`.
  - T03 O18: done — created options stay in the list until `options` includes them; groups wrap their own options.
  - T03 O19: done — Select stays anchored on small screens; an unpositioned layer stays invisible.
  - T03 O20: done — slider direction follows the nearest `dir`, then ConfigProvider; range thumbs add min/max names; display uses `sliderBounds`.
  - T03 O21: done — Stepper is removed; side buttons are InputNumber `controlsPosition="both"`; `snapToStep` defaults off.
  - T03 O22: done — paste inserts at the caret then splits; the highlighted tag is `aria-activedescendant`; only `readOnly` remains.
  - T03 O23: done — disabled fields omit the hidden input; read-only fields submit; an empty selection submits `value=""`.
  - T03 O24: done — slider drag commits on pointerup; marks cap at 21; Select search debounce calls the latest callback.
  - T03 O25: done — FormItem binds value and validation to the first control only.
  - T03 O26: done — Vue Slider is `modelValue` / `update:modelValue` only.
  - T03 O27: done — TagsInput no longer draws `errorMessage` or `_shakeTrigger`; errors go through FormItem.
tests: tsc --noEmit passed for core, vue, and react. vitest 37 files / 755 tests (form engine, Form, FormItem, SchemaForm, FormWizard, Select, Slider, TagsInput, InputOTP, InputNumber, Radio, Checkbox, AutoComplete, Cascader, TreeSelect, locales, a11y, docs-api) passed. vitest CropUpload + anchored-overlay: 3 files / 23 tests passed. pnpm exports:sync, api:baseline, docs:api, mcp snapshot sync, and exports:check passed.
outputs_summary: W3 form basics (T03 O1–O27) are the only behavior; Stepper and the Slider value alias are gone, and skills, examples, and the migration notes match.
blocked_reason:
notes: W3 only. Do not start W4. No bot-to-bot messages. O19 also keeps unpositioned fullscreen-sm and bottom-sheet-sm layers hidden until data-positioned; Select itself stays anchored. No T03 E1–E8.
