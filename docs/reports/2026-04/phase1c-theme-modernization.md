# Phase 1C — 主题现代化剩余验收

> 2026-05-05 执行：完成默认主题像素回归。新增 `e2e/default-theme-visual.spec.ts`，覆盖 Vue / React 的 Button、Input、Card、Form 默认主题截图，并复用已有 overlay 视觉回归覆盖 Modal、Drawer、Popover。Chromium 基线已生成并复跑通过。

| 任务             | 对应组件 / 范围                                                            | 优先级 | 状态 | 完成标准                                                    |
| ---------------- | -------------------------------------------------------------------------- | ------ | ---- | ----------------------------------------------------------- |
| 默认主题像素回归 | Button / Input / Card / Form / Modal / Drawer / Popover 等核心默认主题组件 | P1     | Done | 默认配置与 v1.0.x 视觉保持一致，完成核心组件截图/像素差验证 |
